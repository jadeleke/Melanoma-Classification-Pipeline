# PyTorch and CUDA (GPU build)
# Use the CUDA 13.0 (cu130) wheel index so pip resolves GPU-enabled wheels.
# Other packages will still be fetched from PyPI.
--find-links https://download.pytorch.org/whl/cu130
torch==2.9.0
torchvision>=0.15.0

# Deep learning libraries
timm>=0.9.0                    # EfficientNet, HRNet and other models
albumentations>=1.3.0          # Data augmentation
opencv-python>=4.8.0           # Image processing

# Medical imaging
pydicom>=2.4.0                 # DICOM file support
pillow>=10.0.0                 # Image processing

# Scientific computing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualization and logging
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# New dependencies for hybrid model
# Note: HRNet is available through timm library
# StyleGAN2 (optional - only needed if using synthetic augmentation)
# stylegan2-pytorch>=1.0.0     # Uncomment if using StyleGAN augmentation

# Optional: experiment tracking
# wandb>=0.15.0
# tensorboard>=2.13.0
